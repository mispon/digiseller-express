version: '3.1'

services :
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db
    links:
      - db:database
    environment:
      SELLER_ID: 257605
      SELLER_API_KEY: 912E623E174D40EFAC3DFE8E34BA3F50
      DATABASE_URL: postgres://admin:admin@database:5432/digi

  db:
    image: postgres:15.2-alpine
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
      - ./create_tables.sql:/docker-entrypoint-initdb.d/create_tables.sql
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: digi

  admin:
    image: adminer
    restart: always
    depends_on:
      - db
    ports:
      - "8084:8080"

volumes:
  dbdata:
    driver: local